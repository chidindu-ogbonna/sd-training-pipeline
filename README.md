# Scripts

## Setup

```shell
# !pip install -U -qq git+https://github.com/huggingface/diffusers.git
!pip install -qq diffusers accelerate tensorboard transformers ftfy gradio tqdm requests safetensors omegaconf huggingface_hub
!pip install -qq "ipywidgets>=7,<8"
!pip install -qq bitsandbytes
```
